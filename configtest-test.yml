spring:
  application:
    name: configtest

eureka:  
  client: 
    service-url:  
      defaultZone: http://localhost:10001/eureka/
  instance:
    instance-id: configtest10009  # 修改微服务在界面中，右边status中展示的名字
    prefer-ip-address: true  # 鼠标移上去显示ip信息

feign: 
  hystrix: 
    enabled: true       
  
server: 
  port: 10009
#  servlet:
#    context-path: /c1   
  
info: 
  app.name: mall
  company.name: nbs
  build.artifactId: $project.artifactId$
  build.version: $project.version$  

      
logging:
  level:
    org.springframework.cloud.openfeign: debug
